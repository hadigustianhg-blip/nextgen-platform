import { NextResponse } from "next/server";
import { createFinancialCategory, financialCategorySchema, listFinancialCategories, requireSettingsApi, settingsApiError } from "@/modules/settings";
export async function GET() { const access = await requireSettingsApi(); if ("response" in access) return access.response; try { return NextResponse.json({ success: true, data: await listFinancialCategories(access.scope) }); } catch (error) { return settingsApiError(error); } }
export async function POST(request: Request) { const access = await requireSettingsApi(); if ("response" in access) return access.response; const parsed = financialCategorySchema.safeParse(await request.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ success: false, error: { code: "VALIDATION_ERROR" } }, { status: 400 }); try { return NextResponse.json({ success: true, data: await createFinancialCategory(access.scope, parsed.data) }, { status: 201 }); } catch (error) { return settingsApiError(error); } }
